services:
    app:
        build:
            context: .
        container_name: buehl-landing-app
        ports:
            - '8080:8000'
        volumes:
            - ./:/app
        env_file:
            - .env
        depends_on:
            keycloak:
                condition: service_started
            chat:
                condition: service_started

    chat:
        build:
            context: ./chat-server
        container_name: buehl-chat-server
        environment:
            PORT: 3000
            CHAT_ALLOWED_ORIGIN: http://localhost:8080
        ports:
            - '8082:3000'

    keycloak:
        image: quay.io/keycloak/keycloak:26.1.3
        container_name: buehl-keycloak
        command:
            - start-dev
            - --import-realm
            - --http-port=8080
        environment:
            KC_BOOTSTRAP_ADMIN_USERNAME: admin
            KC_BOOTSTRAP_ADMIN_PASSWORD: admin
            KEYCLOAK_ADMIN: admin
            KEYCLOAK_ADMIN_PASSWORD: admin
            KC_DB: dev-file
        volumes:
            - ./docker/keycloak/realm-export.json:/opt/keycloak/data/import/realm-export.json:ro
        ports:
            - '8081:8080'

